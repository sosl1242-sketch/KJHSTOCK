# 주가 자동 수집 설계 메모

이 프로젝트의 현재가 자동 수집은 Manus 내장 Data API의 `YahooFinance/get_stock_chart`를 사용한다. 한국 상장 주식은 종목코드 뒤에 KOSPI의 경우 `.KS`, KOSDAQ의 경우 `.KQ` 접미사를 붙여 조회하며, 서버 라우터는 저장된 시장 접미사를 우선 사용하고 실패 시 반대 시장 접미사를 재시도하도록 설계했다.

현재 구현 범위는 관리자가 버튼으로 종목별 또는 전체 현재가를 갱신하는 방식이다. 배포 이후 완전한 주기 갱신이 필요하면 프로젝트 지침에 따라 `/api/scheduled/*` 형태의 Heartbeat 엔드포인트를 추가하고, 체크포인트 저장 후 사용자가 배포한 뒤 플랫폼 cron을 생성해야 한다. 인프로세스 타이머나 `node-cron` 방식은 사용하지 않는다.

EPS는 외부 API에서 확정적으로 가져오지 않고, 관리자가 연간 EPS 값을 직접 입력·수정하는 방식으로 병행 제공한다. EPS/주가(%)는 `EPS ÷ 현재 주가 × 100`으로 계산하며, 현재 주가가 0 이하이거나 EPS 값이 유효하지 않으면 계산 결과를 비워 둔다.
