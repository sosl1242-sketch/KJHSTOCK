# 공개 주소 및 미리보기 진단 기록

사용자가 제공한 공개 주소 `https://korstockanaly-9inpejdf.manus.space/`는 브라우저에서 Manus 로그인 흐름으로 이동했으며, 사용자가 앞서 본 예전 EPS/주가 화면은 최신 체크포인트가 아직 공개 배포본에 반영되지 않은 상태일 가능성이 높다.

개발 미리보기 주소에서는 최신 KOSPI 200 화면이 정상적으로 렌더링되었다. 화면에는 `KOSPI 200 밸류에이션·실적 테마 분석`, 200개 종목, EPS/주가 평균 4.15%, PER·PBR·RSI 상세 지표, 엑셀형 헤더 정렬, EPS 및 EPS/주가 컬럼, 종목 상세 진입 안내가 표시된다.

사용자가 추가로 첨부한 화면에는 하단에 `Preview mode` 및 `This page is not live and cannot be shared directly. Please publish to get a public link.` 안내가 보여, 해당 화면은 공개 배포본이 아니라 미리보기/관리 화면임을 확인했다. 최신 내용을 공개 주소에 반영하려면 최신 체크포인트를 기준으로 사용자가 관리 UI에서 Publish를 눌러야 한다.

남은 점검 대상은 테이블의 `요약 수집 중` 또는 `요약 실패`가 반복 표시되는 원인이다. 이는 재무 요약 수집 절차가 외부 금융 페이지에서 PER, PBR, 시가총액, 영업이익을 가져오지 못할 때 발생하는 표시일 가능성이 있다.

## 2026-05-11 추가 수정 및 검증

개발 미리보기 서버는 최신 KOSPI 200 테마 대시보드 화면을 정상적으로 렌더링했다. 화면 상단에는 `KOSPI 200 밸류에이션·실적 테마 분석` 제목과 `PER·PBR·RSI` 지표 요약이 표시되며, TypeScript 검사와 프로덕션 빌드도 통과했다.

공개 도메인 `https://korstockanaly-9inpejdf.manus.space`를 다시 직접 열었을 때는 Manus 로그인 화면으로 이동했다. 따라서 현재 브라우저 세션에서는 로그인된 공개 세션 또는 게시 후 접근 권한 확인 없이는 공개 화면의 최신/이전 UI를 직접 비교하기 어렵다. 사용자가 공개 도메인에서 예전 EPS 섹터 화면을 본 경우, 최신 체크포인트를 아직 게시하지 않았거나 브라우저 캐시가 이전 정적 번들을 유지한 상황일 가능성이 높다.

테이블 영역은 대시보드 본문이 전체 페이지를 가로로 밀지 않도록 `DashboardLayout`의 본문에 `min-w-0`과 `overflow-x-hidden`을 적용했고, KOSPI 200 종목 테이블은 카드 내부에서만 가로 스크롤되도록 조정했다. 또한 테이블 최소 폭을 낮춰 크롬 확대 상태와 중간 폭 화면에서 레이아웃이 덜 깨지도록 수정했다.

요약 재무지표 수집 실패 시 테이블에 `요약 실패`가 반복적으로 노출되던 부분은 `상세 조회` 버튼형 CTA로 바꿨다. 실패 원문은 셀의 `title` 속성에 보존해 사용자가 원인을 확인할 수 있게 했고, CTA를 누르면 해당 종목의 상세 모달에서 재무 상세와 보조지표 조회를 다시 시도하도록 했다.

테이블 안내 문구에는 현재 최신 화면의 식별 표식인 `KOSPI 200 테마·PER/PBR·RSI`와 크롬에서 이전 EPS 화면이 보일 때 `Ctrl+Shift+R` 강력 새로고침을 시도하라는 안내를 추가했다.

## 2026-05-11 공개 도메인 재점검

공개 도메인 `https://korstockanaly-9inpejdf.manus.space`를 다시 열어 확인한 결과, 현재 브라우저는 `https://manus.im/app-auth?...` 로그인 화면으로 리다이렉트된다. 즉 공개 도메인은 존재하지만, 앱의 인증 흐름 때문에 비로그인 상태에서는 대시보드 본문이 바로 보이지 않는다. 최신 수정본 검토용 체크포인트는 `manus-webdev://3bf3d8ea`이며, 공개 도메인에 반영하려면 관리 UI에서 최신 체크포인트 기준으로 Publish가 필요하다.
