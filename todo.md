# Project TODO

- [x] 6개 섹터(전력, 방산, 반도체, 반도체 장비, 디스플레이+디스플레이 장비, 투자·증권)를 기준으로 탭/필터 UI를 구현한다.
- [x] 각 섹터별 종목 테이블에 종목명, 종목코드, 현재 주가, EPS(연간), EPS/주가(%) 컬럼을 표시한다.
- [x] EPS/주가(%)를 EPS ÷ 현재 주가 × 100 방식으로 계산하고 퍼센트 형식으로 표시한다.
- [x] 섹터 내 종목별 EPS/주가(%)를 Recharts 막대 차트로 시각화한다.
- [x] 종목 데이터의 섹터, 종목명, 종목코드, 현재 주가, EPS를 DB에 저장하는 스키마를 구현한다.
- [x] 오너 전용으로 종목 데이터를 추가·수정·삭제할 수 있는 관리 기능을 구현한다.
- [x] 일반 사용자와 오너 권한을 구분하여 편집 기능 접근을 제한한다.
- [x] EPS/주가(%) 기준 오름차순·내림차순 정렬 기능을 구현한다.
- [x] 사이드바 네비게이션 기반 내부 분석 대시보드 레이아웃을 구현한다.
- [x] 미니멀한 스칸디나비안 스타일, 쿨 그레이 배경, 굵은 산세리프 타이포그래피, 파스텔 블루·블러시 핑크 추상 도형 포인트를 적용한다.
- [x] 한국 주식 현재 주가를 외부 Data API 연동 방식으로 자동 수집하는 서버 기능을 구현한다.
- [x] 자동 수집과 병행하여 관리자가 주가와 EPS를 수동 입력·수정할 수 있는 편집 UI를 제공한다.
- [x] 관련 서버 로직과 수익률 계산 로직에 대한 Vitest 테스트를 작성하고 실행한다.
- [x] 구현 완료 후 상태 점검을 수행하고 최종 체크포인트를 저장한다.

- [x] 종목 정보가 비어 보이는 원인을 점검하고 초기 종목 데이터가 화면에 표시되도록 수정한다.
- [x] 각 종목에 데이터 출처, 마지막 갱신 시각, 자동 연동 상태를 표시하는 항목을 추가한다.
- [x] 주요 섹터별 대표 종목 기본 목록을 DB에 자동 시드하여 첫 화면에서 분석 데이터가 보이도록 한다.
- [x] 현재가 자동 수집 실패 시 사용자에게 실패 사유와 수동 업데이트 가능 여부를 명확히 보여준다.
- [x] 종목 정보 연동 개선 후 타입 검사와 Vitest 테스트를 다시 실행하고 체크포인트를 저장한다.

- [x] 기존 6개 제안 섹터 중심 분류를 자체 산업·비즈니스 테마 체계로 확장한다.
- [x] KOSPI 상위 200개 종목 목록을 신뢰 가능한 데이터 출처 기준으로 확보해 기본 종목 데이터에 포함한다.
- [x] 각 종목에 자체 테마, 종목코드, 시장구분, 현재가, EPS, EPS/주가(%), 데이터 출처, 마지막 갱신 시각을 표시한다.
- [x] 테마별 탭/필터가 KOSPI 200개 종목 규모에서도 사용 가능하도록 검색과 정렬 흐름을 개선한다.
- [x] 대량 종목 기본 데이터가 비어 보이지 않도록 DB 시드 및 기존 DB 보강 로직을 수정한다.
- [x] KOSPI 200개 종목 확장 후 타입 검사와 Vitest 테스트를 실행하고 새 체크포인트를 저장한다.

- [x] Home.tsx 테이블에 자체 테마, 시장구분, 데이터 출처, 마지막 갱신 시각, 자동 연동 상태를 명시적으로 렌더링한다.
- [x] 현재가 자동 수집 실패 결과를 UI에서 표시하고 실패 사유와 관리자 수동 수정 가능 안내를 제공한다.
- [x] KOSPI 200 확장 및 UI 보강 후 타입 검사, Vitest 테스트, 상태 점검, 새 체크포인트 저장을 완료한다.

- [x] 히어로 제목의 과도한 줄바꿈과 글자 겹침을 해결하고 화면 폭별로 자연스럽게 보이도록 재정리한다.
- [x] EPS/주가 도표의 축 라벨, 막대 라벨, 툴팁이 겹치지 않도록 차트 레이아웃과 표시 개수를 개선한다.
- [x] 테마 탭, 검색 영역, 요약 카드가 작은 화면과 중간 화면에서 겹치지 않도록 반응형 레이아웃을 보강한다.
- [x] 종목 테이블의 가독성을 높이고 컬럼 폭·뱃지·긴 텍스트가 깨지지 않도록 정리한다.
- [x] 개선 후 타입 검사, Vitest, 상태 점검, 새 체크포인트 저장을 완료한다.

- [x] 종목 목록의 핵심 지표를 EPS 중심에서 PER, PBR, 시가총액, 영업이익 중심으로 확장한다.
- [x] 종목을 클릭하면 세부 정보 패널 또는 모달이 열리고 주요 밸류에이션·수익성 지표가 한 화면에 표시되도록 구현한다.
- [x] 상세 정보에 최근 분기별 매출, 영업이익, 순이익 표를 표시한다.
- [x] 재무지표 자동 수집 실패 시 사유와 수동 확인 안내를 표시한다.
- [x] 기존 차트와 히어로 영역의 글자 겹침을 해결하고 PER/PBR/시가총액 관점에 맞게 도표를 재구성한다.
- [x] 신규 재무지표 서버 로직과 상세 조회 절차에 대한 Vitest 테스트를 추가 또는 갱신한다.

- [x] 종목 테이블 각 행에 실제 PER, PBR, 시가총액, 최근 영업이익 값을 표시하도록 요약 재무지표 데이터와 UI를 연결한다.
- [x] 재무 상세 조회 실패 UI에 사용자가 네이버 금융 원문 등에서 수동 확인할 수 있는 명시적 안내 문구와 링크/경로를 추가한다.
- [x] 차트를 PER/PBR/시가총액 중 최소 하나 이상의 실제 재무지표 기반 시각화로 재구성하고 해당 지표를 툴팁과 축에 반영한다.

- [x] 테이블 헤더 클릭 시 각 컬럼이 내림차순, 오름차순, 정렬취소 순서로 순환하도록 엑셀형 3단계 정렬을 구현한다.
- [x] EPS와 EPS/주가(%) 지표가 테이블과 요약 화면에서 다시 명확히 보이도록 데이터 흐름과 UI 표시를 점검·복원한다.
- [x] 사용자가 현재 정렬 상태와 기준 컬럼을 쉽게 이해하도록 헤더 아이콘, 보조 문구, 빈 값 처리, 모바일/가로 스크롤 안내를 개선한다.
- [x] 전체 대시보드의 필터, 차트, 표, 상세 모달을 더 UI 친화적으로 재정리하고 과도한 정보 밀도를 낮춘다.
- [x] 정렬·EPS 복원·UI 개선 후 TypeScript 검사, Vitest 테스트, 상태 점검, 새 체크포인트 저장을 완료한다.
- [x] 종목 상세 모달에 RSI를 포함한 고점·저점 판단용 대표 보조지표 10개를 추가한다.
- [x] 보조지표 산출에 필요한 최근 주가 이력 조회 서버 로직과 지표 계산 로직을 구현한다.
- [x] 보조지표별 현재 값, 해석, 과열·중립·침체 상태를 사용자 친화적으로 표시한다.
- [x] 보조지표 계산 로직에 대한 Vitest 테스트를 추가하고 기존 테스트와 함께 검증한다.
- [x] 모든 개선이 끝난 뒤 사용자가 바로 접속할 수 있는 공개 사이트 주소를 최종 결과에 다시 포함한다.

- [x] 공개 주소를 크롬에서 열었을 때 화면이 이상하게 보이는 원인을 확인하고 레이아웃·호환성 문제를 수정한다.
- [x] 반복 표시되는 ‘요약 실패’ 메시지의 서버·클라이언트 원인을 진단하고 사용자 친화적인 오류 처리 또는 대체 표시를 구현한다.
- [x] 크롬 표시 문제와 요약 실패 수정 후 TypeScript 검사, Vitest 테스트, 상태 점검, 새 체크포인트 저장을 완료한다.

- [x] 공개 도메인에서 최신 KOSPI 200 화면이 아니라 예전 EPS/주가 섹터 화면이 표시되는 원인을 확인하고 최신 체크포인트 반영 경로를 정리한다.
- [x] 크롬에서 공개 사이트가 오래된 캐시 또는 이전 배포본을 표시할 때 사용자가 혼동하지 않도록 안내와 화면 상태를 점검한다.
- [x] 종목 요약 수집 실패가 반복될 때 테이블에 ‘요약 실패’만 노출하지 않고 원인, 재시도, 상세 확인 경로를 더 친절하게 표시하도록 개선한다.
- [x] 공개 화면 불일치와 요약 실패 처리 개선 후 TypeScript 검사, Vitest 테스트, 상태 점검, 새 체크포인트 저장을 완료한다.
- [x] 공개 도메인과 최신 체크포인트 링크를 다시 확인하고 사용자에게 정확한 접속 링크를 전달한다.
- [x] PER과 PBR이 나눗셈처럼 보이지 않도록 화면 전반에서 별도 지표·별도 라벨로 분리 표기한다.
- [x] PER/PBR 분리 표기 수정 후 TypeScript 검사, Vitest 테스트, 상태 점검, 새 체크포인트 저장을 완료한다.
- [x] 관리 미리보기와 크롬 공개 주소에서 서로 다른 화면이 보이는 원인을 버전·게시·캐시 관점에서 확인하고 사용자에게 명확히 설명한다.
- [x] 전체 보기에서는 시총 기준 최대 300개 테마별 비교 차트를 유지하고, 개별 세부 테마 선택 시 해당 테마의 대표 종목 약 20개를 종목별 막대 차트로 표시한다.
- [x] 테마별 종목 차트 개선 후 관련 회귀 테스트, TypeScript 검사, 프로덕션 빌드, 상태 점검, 새 체크포인트 저장을 완료한다.
- [x] Home.tsx 698번째 줄 근처 JSX 파서 오류 수정으로 Vite 개발 서버 복구
- [x] 보조지표 카드의 외부 가격 이력 요청 실패 오류 수정
- [x] 종목 상세 모달에 일·주·월 시간축을 선택할 수 있는 가격 차트 추가
- [x] 수정 완료 후 체크포인트 저장 및 최신 버전 게시 안내

- [x] 고점·저점 판단 보조지표를 기존 10개에서 최종 12개로 확장한다.
- [x] 보조지표 카드를 클릭하면 지표 의미, 판단 기준, 현재 해석, 주의점이 자세히 보이는 상세 해설을 구현한다.
- [x] 상세 해설에 가격·이동평균선·볼린저밴드·MACD 등 기술적 근거를 차트로 함께 표시한다.
- [x] 보조지표 12개 확장과 상세 해설 차트 구현 후 Vitest, TypeScript 검사, 프로덕션 빌드, 상태 점검, 새 체크포인트 저장을 완료한다.

- [ ] 주식 종목 범위를 KOSPI 시가총액 상위 200개에서 시총 기준 최대 300개로 확대한다.
- [ ] KOSPI 500 확대에 맞춰 화면 제목, 설명 문구, 전체 보기 필터 기준, 차트 라벨을 수정한다.
- [ ] KOSPI 500 확대 후 데이터 조회·테마 집계·테이블 페이지네이션이 정상 동작하는지 검증한다.

- [x] 사이드/상단 내비게이션을 국내주식 섹터분석, 해외주식 섹터분석, 크립토 섹터분석으로 확장한다.
- [x] 첨부 화면 기준 PER, PBR, 시가총액, 연동 상태 등 재무 요약 컬럼 정렬 불가 문제를 수정한다.
- [x] 해외주식 섹터분석 탭에 미국 대형주 12개 지표 테이블과 동일 스타일 요약 카드를 추가한다.
- [ ] 크립토 섹터분석 탭에 바이낸스 선물 상장 코인 전체 정보와 시가총액·공급량·거래량·펀딩비 등 핵심 지표를 추가한다.
- [x] 보조지표 기반 추정 적정가격 중간값을 상세 모달에 표시한다.


- [x] 종목 상세창 상단 핵심 지표 카드를 시가총액, PER, PBR, ROE, EPS, BPS, 영업이익률, 부채비율, 순차입금, 배당수익률, 매출 성장률 YoY, 영업이익 성장률 YoY의 12개 기본 카드로 확장한다.
- [x] 상세창 12개 지표에 각 지표의 투자 판단 이유를 함께 표시하고 데이터가 없을 때 명확한 대체 문구를 제공한다.
- [x] 상세창 핵심 지표 확장에 맞춰 재무 파서 또는 프런트 표시 로직 테스트를 보강한다.

- [x] 사용자 재확인 요청에 따라 고점·저점 판단 보조지표를 현재 10개에서 12개로 우선 확장한다.
- [x] 사용자 재확인 요청에 따라 보조지표별 예상 적정주가 중간값을 상세 모달에 우선 표시한다.

- [x] 사용자 요청에 따라 보조지표 12개 확장, 예상 적정주가 중간값, 상세 해설·근거 차트 구현 상태를 재점검한다.
- [x] 보조지표 재점검 후 필요한 수정이 있으면 반영하고 TypeScript, Vitest, 프로덕션 빌드, 상태 점검을 완료한다.

- [x] 실제 시장 가격을 짧은 시간 단위로 계속 따라가도록 서버 주기 업데이트 구조를 설계한다.
- [x] 장중/장외 상태, API 호출 한도, 실패 재시도, 마지막 갱신 시각을 고려한 가격 캐시 정책을 구현한다.
- [x] 화면이 최신 캐시 가격을 자동으로 다시 조회하도록 새로고침 주기와 수동 새로고침 상태 표시를 추가한다.
- [x] 주기 가격 추적 기능 구현 후 TypeScript, Vitest, 프로덕션 빌드, 상태 점검, 체크포인트 저장을 완료한다.

- [ ] 가격 자동 갱신 API 실패 시 재시도 정책을 서버 로직과 테스트에 추가한다.
- [ ] 자동 가격 추적 및 보조지표 상세 변경사항 반영 후 새 체크포인트를 실제로 저장한다.

- [x] 미국증시 데이터가 현재 화면에서 제대로 표시되지 않는 문제를 진단하고 수정한다.
- [x] 바이낸스 선물 데이터가 현재 화면에서 제대로 표시되지 않는 문제를 진단하고 수정한다.
- [x] 미국증시·바이낸스 선물 수정 후 관련 테스트, TypeScript 검사, 프로덕션 빌드, 상태 점검, 체크포인트 저장을 완료한다.

- [ ] 개선안 1: 해외주식 섹터분석 화면에 대표 미국주식 실시간 가격·등락률·거래대금 기반 요약 카드와 표를 연결한다.
- [ ] 개선안 2: 크립토 섹터분석 화면에 바이낸스 USDT 선물 티커·펀딩비·거래대금·섹터 분류 기반 표를 연결한다.
- [ ] 개선안 3: 두 화면 모두 데이터 갱신 시각, 실패 원인, 수동 재시도 버튼, 검색·정렬 UX를 추가해 데이터가 비어 보이는 문제를 줄인다.
- [ ] 3개 개선안 반영 후 서버 로직 테스트, TypeScript 검사, 프로덕션 빌드, 상태 점검, 체크포인트 저장을 완료한다.

- [x] 보조지표 카드 클릭 시 상세 분석창이 열리는지 점검하고 누락된 클릭 인터랙션을 복구한다.
- [x] 상세 분석창에 보조지표의 종류, 계산 방식, 해석 기준, 현재 종목의 상태 분석을 명확히 표시한다.
- [x] 상세 분석창에 가격·이동평균·볼린저밴드·MACD 등 지표별 근거 차트를 함께 제공한다.
- [x] 보조지표 상세 분석창 보강 후 관련 테스트, TypeScript 검사, 프로덕션 빌드, 상태 점검, 체크포인트 저장을 완료한다.

- [x] 비밀번호 인증 페이지 추가: '5690' 입력 시 세션 쿠키 설정, 누구나 접속 가능하게 변경

- [x] GitHub 최신 변경 사항을 현재 프로젝트에 동기화하고 충돌·타입 오류 여부를 확인한다.

- [ ] 동료가 수정한 해외주식·크립토 섹터 변경분을 현재 국내주식 섹터 구조와 충돌 없이 통합한다.
- [ ] 크립토 섹터에서 바이낸스 USDT 선물시장 상위 100개 종목을 실제 API 기준으로 가져오고 거래대금·등락률·펀딩비를 표시한다.
- [ ] 해외주식 섹터 화면을 국내주식 섹터처럼 요약 카드, 섹터/테마 필터, 정렬 가능한 표, 상세 분석 흐름으로 재구성한다.
- [ ] 해외주식·크립토 통합 후 빈 상태, 실패 원인, 마지막 갱신 시각, 수동 재시도 UX를 정리한다.
- [ ] 통합 작업 후 Vitest, TypeScript 검사, 상태 점검, 체크포인트 저장을 완료한다.

- [ ] 국내·해외주식·크립토 3개 섹터분석에 전체 초기화 대신 부분 캐시 갱신 정책을 적용한다. 현재 국내주식·크립토·해외주식 화면 쿼리 staleTime 조정은 반영했으나, 3개 화면 전체의 서버 실패 캐시 보장까지 테스트로 고정해야 완료 처리한다.
- [ ] 국내·해외주식·크립토별로 가격·거래량·펀딩비·재무지표의 갱신 주기를 분리하고 실패 시 마지막 성공 데이터를 유지한다. 현재 국내주식 가격/재무 캐시와 크립토 실패 캐시는 확인했으나, 해외주식 서버 캐시 보강이 남아 있다.
- [x] 해외주식 상세 모달에 국내주식과 동일한 고점·저점 판단 보조지표 12개와 상세 해설을 구현한다.
- [x] 크립토 상세 모달에 고점·저점 판단 보조지표 12개와 상세 해설을 구현한다.
- [x] 해외주식·크립토 가격 차트를 일봉 최근 6개월, 주봉 최근 2년, 월봉 최근 3년 범위로 확장한다.
- [x] 3개 섹터분석 통합 레시피 적용 후 Vitest, TypeScript 검사, 프로덕션 빌드, 상태 점검, 체크포인트 저장을 완료한다.

- [x] 국내주식 상세 분석 가격 차트도 일봉 최근 6개월, 주봉 최근 2년, 월봉 최근 3년 범위로 확장한다.

- [x] 크립토 섹터분석의 범위를 Binance USDT 선물 상위 100개가 아니라 USDT 선물시장 전체 종목으로 확장한다.
- [ ] USDT 선물시장 전체 종목 표시를 위해 검색, 섹터 필터, 정렬, 페이지 처리 또는 표시 제한 UX를 성능 저하 없이 구현한다.

- [ ] 이전 ‘맥스 표현 제외’ 요청 철회에 따라 사용자-facing 문구 정리 범위에서 해당 제약을 제거하고 기존 분석 기능 구현을 우선한다.

## 2026-05-12 팀 공동 작업용 상세 TODO — 600포인트 내 우선순위

아래 항목은 현재 동료들과 동시에 수정 중인 상황을 전제로, **충돌을 줄이고 포인트를 아끼기 위해 구현 위치와 완료 기준을 명확히 남긴 작업 목록**입니다. 구현 순서는 `Home.tsx`의 차트 기간 필터처럼 비용이 낮고 파급이 작은 변경을 먼저 처리한 뒤, 해외주식·크립토 상세 모달의 서버 기반 12개 보조지표 연결로 이동하는 방식이 가장 안전합니다.

| 우선순위 | 상태 | 작업 | 담당 파일·수정 위치 | 구체적 수정 방법 | 완료 기준 |
| --- | --- | --- | --- | --- | --- |
| 1 | [x] | 국내주식 상세 분석 장기 차트 범위 확장 | `client/src/pages/Home.tsx`의 `priceChartData` `useMemo` 구간. 기존 컨텍스트 기준 약 612~645줄이며, 현재 파일에서 `priceChartFrame === "daily"`, `weekly`, `monthly` 분기 검색 | 일봉은 `history.slice(-126)`으로 최근 약 6개월 거래일만 사용한다. 주봉은 주 단위 집계 후 `Array.from(grouped.values()).slice(-104)`로 최근 2년 104주만 사용한다. 월봉은 월 단위 집계 후 `Array.from(grouped.values()).slice(-36)`로 최근 3년 36개월만 사용한다. 기존 차트 UI와 상태명은 유지해 충돌을 줄인다. | 국내주식 상세 모달에서 일·주·월 버튼 전환 시 각각 최근 6개월, 2년, 3년 범위만 렌더링되며 기존 보조지표 카드와 상세 해설 모달이 깨지지 않는다. |
| 2 | [x] | 크립토 종목 범위를 Binance USDT 선물 상위 100개에서 전체 USDT 선물시장으로 확장 | `server/cryptoFutures.ts`에서 `TOP_N`, `slice(0, 100)`, `limit` 또는 “상위 100개” 처리 검색. `client/src/pages/CryptoSectors.tsx`의 설명 문구 약 326~328줄도 함께 수정 | 서버에서는 Binance Futures의 USDT 무기한 선물 심볼 전체를 대상으로 하되, `status === "TRADING"`, `quoteAsset === "USDT"`, 가능하면 perpetual 계약만 필터링한다. 단순 상위 100개 제한 상수는 제거한다. 클라이언트는 전체 데이터가 많아질 수 있으므로 기존 검색·섹터 필터·정렬을 유지하고, 화면 설명의 “상위 100개” 문구를 “전체 USDT 선물시장”으로 바꾼다. | 요약 카드의 분석 코인 수가 100개 제한에 묶이지 않고, 크립토 표·섹터 차트가 전체 USDT 선물 종목 기반으로 계산된다. |
| 3 | [x] | 해외주식 상세 모달을 서버 기반 12개 고점·저점 보조지표로 교체 | `client/src/pages/GlobalStocks.tsx`. `selectedStock`, 상세 `Dialog`, 로컬 지표 계산, 단기 차트 데이터 생성 구간 검색. 서버 라우터는 `server/routers.ts`의 `globalStocks.getDetail`, 서버 함수는 `server/usStocks.ts`의 `getGlobalStockDetail` 사용 | `const [priceChartFrame, setPriceChartFrame] = useState<"daily" | "weekly" | "monthly">("daily")` 상태를 추가하거나 기존 동일 상태가 있으면 재사용한다. `trpc.globalStocks.getDetail.useQuery({ ticker: selectedStock?.ticker ?? "", frame: priceChartFrame }, { enabled: Boolean(selectedStock?.ticker), staleTime: 1000 * 60 * 3, refetchOnWindowFocus: false })` 형태로 서버 상세 데이터를 연결한다. 기존 로컬 계산 지표와 단기 차트는 서버 응답의 `technical.indicators`, `technical.chartData`, `technical.fairPriceMedian` 또는 실제 응답 필드명에 맞춰 교체한다. 국내주식 `Home.tsx`의 상세 모달 패턴을 복사하되 종목명·티커·통화 표기만 해외주식에 맞춘다. | 해외주식 종목 클릭 시 상세 모달에서 RSI14, 스토캐스틱, Williams %R, CCI20, MFI14, 볼린저 위치, MACD 히스토그램, 20일선 이격도, 60일선 이격도, 거래량 20일 배율, 52주 고점 대비, 52주 저점 대비의 12개 카드가 서버 값으로 표시된다. |
| 4 | [x] | 해외주식 상세 모달에 일봉 6개월·주봉 2년·월봉 3년 장기 차트 적용 | `client/src/pages/GlobalStocks.tsx`의 상세 모달 차트 영역. 서버 데이터는 `server/usStocks.ts`의 `getGlobalStockDetail`에서 Yahoo Finance 가격 이력과 `server/technicalIndicators.ts` 결과를 반환 | 상세 모달 상단 또는 차트 헤더에 일·주·월 버튼을 추가한다. 버튼 클릭 시 `priceChartFrame`을 바꾸고 `globalStocks.getDetail` 쿼리가 해당 frame으로 재조회되게 한다. 렌더링은 `ComposedChart` 또는 국내주식과 동일한 Recharts 구조를 사용하며 가격, 20일선, 60일선, 볼린저밴드, MACD, 거래량 보조 차트 중 서버 응답에 있는 필드만 안전하게 렌더링한다. | 해외주식 상세 모달에서 기간 버튼이 동작하고, 일봉은 6개월, 주봉은 2년, 월봉은 3년 기준의 장기 가격 흐름과 보조지표 근거가 보인다. |
| 5 | [x] | 크립토 상세 모달에 서버 기반 12개 고점·저점 보조지표 추가 | `client/src/pages/CryptoSectors.tsx`의 `selectedCoin` 상세 `Dialog` 구간. 현재 약 389~395줄에 “상단 핵심 카드 12개”와 “24시간 가격 범위”만 있음. 서버 라우터는 `server/routers.ts`의 `crypto.getDetail`, 서버 함수는 `server/cryptoFutures.ts`의 `getCryptoDetail` 사용 | 기존 Binance 선물 수급 지표 카드(`metricOrder` 기반)는 유지하되, 그 아래에 별도 섹션 “기술적 고점·저점 보조지표 12개”를 추가한다. `trpc.crypto.getDetail.useQuery({ symbol: selectedCoin?.ticker ?? "", frame: priceChartFrame }, { enabled: Boolean(selectedCoin?.ticker), staleTime: 1000 * 60 * 3, refetchOnWindowFocus: false })`를 연결하고 서버 응답의 `technical.indicators`를 카드로 렌더링한다. 기존 Binance 지표 상세 해설 모달과 충돌하지 않도록 기술적 지표 선택 상태는 `selectedIndicatorKey`처럼 별도 state로 분리한다. | 크립토 상세 모달에서 Binance 원천 지표 12개와 별개로 RSI14 등 기술적 보조지표 12개가 표시되고, 서버에서 계산한 상태·해석·예상 적정가가 함께 보인다. |
| 6 | [x] | 크립토 상세 모달에 장기 차트 패널 추가 | `client/src/pages/CryptoSectors.tsx`의 24시간 가격 범위 차트 바로 아래. 기존 `detailChartData`는 24h 원천 가격 범위용으로 유지 | `priceChartFrame` 상태와 일·주·월 버튼을 추가한다. `crypto.getDetail`의 `frame` 입력과 연결해 Binance Klines 기반 장기 데이터를 가져온다. 24시간 범위 차트는 제거하지 말고, 그 아래에 “장기 기술적 차트” 섹션으로 가격·이동평균·볼린저밴드·MACD·거래량 근거 차트를 렌더링한다. | 크립토 상세 모달에서 기존 24시간 범위 차트와 별도로 일봉 6개월, 주봉 2년, 월봉 3년 장기 차트를 확인할 수 있다. |
| 7 | [ ] | 3개 섹터분석 공통 캐시 갱신 정책 정리 | `client/src/pages/Home.tsx`, `client/src/pages/GlobalStocks.tsx`, `client/src/pages/CryptoSectors.tsx`의 tRPC `useQuery` 옵션. 서버 측 캐시는 `server/stocks.ts`, `server/usStocks.ts`, `server/cryptoFutures.ts`의 메모리 캐시 또는 TTL 로직 검색 | 가격·등락률·거래량·펀딩비처럼 빠르게 변하는 데이터는 `staleTime: 1000 * 60 * 3` 정도로 3분 캐시를 적용하고, `refetchInterval`은 제거하거나 5분 이상으로 제한한다. PER/PBR/EPS/시가총액 등 재무·기초지표는 6시간 이상 캐시되도록 서버 TTL 또는 쿼리 staleTime을 분리한다. **1분마다 전체 초기화하는 방식은 금지**하고, 실패 시 마지막 성공 데이터를 유지하도록 한다. | 국내주식·해외주식·크립토 화면이 전체 데이터를 1분마다 비우고 다시 받지 않으며, 가격성 데이터는 짧게 갱신되고 재무성 데이터는 장시간 캐시된다. |
| 8 | [x] | 관련 테스트 최소 갱신 |
| 8-1 | [x] | 해외주식 기술지표 카드 클릭 상세 해설 보강 | `client/src/pages/GlobalStocks.tsx`의 서버 기술지표 카드 영역과 별도 `Dialog` 추가 | 기술지표 카드를 `button`으로 바꾸고 선택 상태를 추가해 의미, 판단 기준, 주의점, 현재 해석, 참고 적정가를 표시한다. 국내주식 상세 해설과 같은 정보 구조를 맞춘다. | 해외주식 기술지표 카드 클릭 시 별도 상세 해설 모달이 열리고 서버 지표의 해석 정보가 표시된다. |
| 8-2 | [x] | 해외주식 장기 차트 기술근거 보강 | `client/src/pages/GlobalStocks.tsx`의 장기 가격 차트 영역 | 가격선 외에 서버 가격 이력으로 계산 가능한 20일 이동평균, 60일 이동평균, 거래량 막대 또는 설명형 근거를 추가한다. 포인트 절약을 위해 MACD/Bollinger 전체 구현이 무거우면 TODO 기준을 현재 구현 범위에 맞게 명확히 축소한다. | 장기 차트가 단순 가격선만이 아니라 최소 하나 이상의 기술근거를 함께 보여준다. |
| 8-3 | [x] | 해외주식 상세 경로 회귀 테스트 추가 | `server/us-stocks.test.ts` 또는 기존 UI 계약 테스트 | 해외주식 상세 API가 12개 지표와 가격 이력을 반환한다는 계약을 테스트로 고정한다. | 관련 테스트가 통과한다. | `server/crypto-futures.test.ts`, `server/us-stocks.test.ts`, `server/stocks.test.ts` 및 필요 시 클라이언트 렌더링 테스트 | 크립토 전체 USDT 선물 범위는 “100개로 자르지 않는다”는 회귀 테스트를 추가한다. 해외주식·크립토 상세 함수는 `getDetail`이 12개 보조지표와 차트 데이터를 반환하는지 확인한다. 포인트 절약을 위해 새 테스트는 핵심 회귀만 추가하고, 기존 테스트를 과도하게 재작성하지 않는다. | `pnpm test` 또는 관련 테스트 파일 단위 실행에서 새 회귀가 통과한다. |
| 9 | [x] | 최종 검증과 체크포인트 저장 | 프로젝트 루트 `/home/ubuntu/korea-stock-sector-analyzer` | 구현 후 `pnpm check`, 관련 `pnpm test`, 필요 시 `pnpm build`를 실행한다. 모든 완료 항목은 이 파일에서 `[x]`로 바꾼 뒤 체크포인트를 저장한다. 포인트가 부족하면 최소한 TypeScript 검사와 변경 파일 중심 테스트를 우선한다. | 타입 오류와 핵심 회귀 오류가 없고, `todo.md` 완료 상태가 실제 구현 상태와 일치하며, 새 체크포인트가 생성된다. |

주의사항: 현재 공동 작업 중이므로 **대형 리팩터링, 파일 구조 변경, 함수명 변경은 피하고**, 기존 `Home.tsx` 국내주식 상세 분석 패턴을 해외주식·크립토에 얇게 복사·적용하는 방식이 가장 안전합니다. 특히 `server/technicalIndicators.ts`가 이미 12개 보조지표를 계산하므로, 클라이언트에서 동일 지표를 다시 계산하지 말고 서버 상세 API 응답을 그대로 렌더링해야 합니다.


## 2026-05-13 공개 접근 + 매일 12시 자동 갱신

- [x] OAuth 로그인 제거 및 공개 접근 활성화
- [x] 매일 12시(KST) 자동 데이터 갱신 스케줄 작업 설정
- [x] 모든 주식 데이터 조회 프로시저를 publicProcedure로 변경 (로그인 프롬프트 제거)
- [x] DashboardLayout에서 useAuth 제거 및 사용자 프로필 드롭다운 제거
- [x] Home.tsx에서 useAuth 제거 및 isAdmin = false 설정
- [x] 오너 편집 패널을 {isAdmin ? (...) : null} 조건부 렌더링으로 숨김
- [x] 관리자 전용 프로시저(adminProcedure) 검증 완료
- [x] 공개 조회 vs 관리자 수정 API 분리 확인 완료
- [x] 모든 테스트 37개 통과
- [x] 공개 접근 + 자동 갱신 최종 검증 및 체크포인트 저장
